import React from 'react';
import { ScreenPlaceholder } from '@components/ScreenPlaceholder';

/**
 * Projects list screen — main entry point of the app.
 */
export default function ProjectsScreen(): JSX.Element {
  return (
    <ScreenPlaceholder
      title="Projects"
      subtitle="Your site audits and inspections will appear here."
    />
  );
}
